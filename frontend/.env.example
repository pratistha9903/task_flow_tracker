# Frontend API URL (required for production deploy on Render/Vercel/Netlify)
# Must be set BEFORE running npm run build — Vite bakes this into the bundle.
#
# Example Render backend:
# VITE_API_URL=https://your-backend-name.onrender.com/api/v1

VITE_API_URL=http://localhost:3001/api/v1
